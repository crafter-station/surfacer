[package]
name = "surfacer-emit-cli"
edition.workspace = true
version.workspace = true

[dependencies]
anyhow.workspace = true
camino.workspace = true
serde.workspace = true
serde_json.workspace = true
owo-colors.workspace = true
url.workspace = true
surfacer-ir = { path = "../surfacer-ir" }

[dev-dependencies]
tempfile.workspace = true
# The cross-emitter golden test runs the same selectors through scraper that
# `surfacer-app` runs, so the Rust side of the comparison is the real engine
# rather than a description of it.
scraper.workspace = true
